I am building a React web application called LS.AI.Catalog — a knowledge 
repository for the Accenture Life Sciences AI & Data practice. I have a 
complete UX specification document that defines every screen, component, 
design token, and piece of mock data needed. I will share it now.

Your job in this first step is to:

1. Scaffold the project structure exactly as defined in Section 6.2 of the 
   spec (component folders, pages, data files, hooks, styles)

2. Implement the design tokens from Section 2 in full as CSS custom 
   properties in src/styles/tokens.css

3. Set up routing in App.jsx per Section 6.1 using React Router — four 
   routes: /, /search, /browse/:domainId, /contribute

4. Create the NavBar component (Section 3.1) — fully styled, with the 
   LS.AI wordmark, placeholder intent bar slot (not functional yet), and 
   Contribute an asset button

5. Create the PageLayout component that wraps all inner pages with the 
   fixed nav and correct page padding

Do not build any screens yet. Do not add any libraries beyond React Router. 
Stop after completing these five tasks and confirm what has been built before 
proceeding.

Here is the full specification:

[PASTE FULL SPEC HERE]
